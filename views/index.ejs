<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hello World</title>
</head>
<body>
    <h1>Hello World!</h1>
    <% if (user) { %>
        <p>Welcome, <%= user.displayName %>!</p>
        <a href="/logout">Logout</a>
    <% } else { %>
        <a href="/auth/ping">Login with Ping</a>
    <% } %>
</body>
</html>
